
language: python

python:
    - 2.7

before_install:
    - pip install -r test-requirements.txt

install:
  - "pip install ."
  - "pip install -r requirements.txt"
  - "pip install coveralls"

# command to run tests
script: nosetests --with-coverage --cover-package=ontimer

after_success:
  coveralls
